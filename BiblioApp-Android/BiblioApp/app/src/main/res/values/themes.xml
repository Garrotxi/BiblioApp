<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema basico App. -->
    <style name="Theme.BiblioApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">

        <!-- Colores primarios -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Colores primarios -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Color barra de estado -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>


        <!-- Transiciones -->
        <!-- Se habilitan transiciones -->
        <item name="android:windowActivityTransitions">true</item>

        <!-- trnasiciones de entrada y de salida -->
        <item name="android:windowEnterTransition">@transition/explode</item>
        <item name="android:windowExitTransition">@transition/explode</item>

        <!-- transicion de elemento compartido -->
        <item name="android:windowSharedElementEnterTransition">
            @transition/change_image_transform
        </item>
        <item name="android:windowSharedElementExitTransition">
            @transition/change_image_transform
        </item>
    </style>


    <!-- El tema de la splash screen -->
    <style name="AppTheme.Splash" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_drawable</item>
    </style>

    <!-- El tema sin Action Bar -->
    <style name="Theme.BiblioApp.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Temas para sobreponer popup y action bar-->
    <style name="Theme.BiblioApp.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.BiblioApp.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>